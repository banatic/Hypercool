{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "기본 권한: core 기본 + dialog open 허용",
  "windows": [
    "*"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:default",
    "shell:allow-open",
    "deep-link:default",
    "core:window:allow-hide",
    "core:window:allow-show"
  ]
}